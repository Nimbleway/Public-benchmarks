# Secrets — never commit. .env.example is the template that IS committed.
.env
.env.*
!.env.example

# Benchmark output and logs. runs/ is the provenance for published numbers;
# it stays local (see AGENTS.md).
runs/
logs/

# Generated leaderboard render artifacts (`make leaderboard-preview`,
# `make leaderboard-snapshot`).
_preview/
data/leaderboard_sample.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Editors / OS
.idea/
.vscode/
.DS_Store
