---
title: "Web API Leaderboard"
description: "Head-to-head SimpleQA benchmark of web search and answer APIs: accuracy, latency, and statistical significance vs the Nimble baseline."
icon: "chart-line"
---

{/* Auto-generated by `python -m nimble_benchmark.leaderboard ... --public-docs-target`.
    Do not edit by hand; rerun the generator and overwrite.

    Reproduction commands, per-sampler config knobs, raw run artifacts, and
    the longer methodology note are intentionally NOT inlined here -- this
    page is the public competitor scoreboard, so the only sections are the
    headline table and the per-system significance detail table that explains
    ★ markers. Operators who need to reproduce a run pull the workflow
    artifacts (`leaderboard.md`, `run.json`, `sampler_config_*.json`, raw
    CSVs) attached to the GitHub Actions run that generated this MDX.

    Run-provenance (run name, n, synthesis model, generated-at timestamp)
    used to render as a callout at the bottom of this page. It was removed
    so the public scoreboard ends at the data. The same provenance still
    lives in `leaderboard.md`'s heading prelude and in `run.json` for
    anyone tracing a published number back to its run. */}

$leaderboard_section